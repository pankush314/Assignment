pipeline {
    agent any

    stages {
        stage('Push to develop') {
            steps {
                echo 'Hello World'
            }
        }
	stage('Push to test') {
            steps {
                echo 'Hello World'
            }
        }
	stage('Push to Prod') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
